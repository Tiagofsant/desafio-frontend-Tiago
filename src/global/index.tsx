import { Stack, styled } from "@mui/material";

// ------------------------------------------------------------------

export const StyledContentRow = styled(Stack)(() => ({
  display: "flex",
  flexDirection: "row",
  gap: 10,
}));
